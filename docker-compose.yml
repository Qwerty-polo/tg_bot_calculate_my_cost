services:
  bot:
    build: .
    container_name: expense-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      # Override to store the DB on the mounted volume.
      DATABASE_URL: sqlite+aiosqlite:///./data/expenses.db
    volumes:
      - ./data:/app/data
